name: build-engine
on: [push] 
jobs: 
  build-emsdk:
    runs-on: ubuntu-latest
    steps:
      - uses: mymindstorm/setup-emsdk@v12
      - name: run emcc  
        working-directory: ./engine
      - run: emcc -embind -o engine.js engine/engine.cpp
